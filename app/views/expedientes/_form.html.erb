<%= form_with(model: expediente, local: true) do |form| %>
  <% if expediente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expediente.errors.count, "error") %> prohibited this expediente from being saved:</h2>

      <ul>
      <% expediente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :escuela %>
    <%= form.text_field :escuela %>
  </div>

  <div class="field">
    <%= form.label :alumno %>
    <%= form.text_field :alumno %>
  </div>

  <div class="field">
    <%= form.label :numero_boleta %>
    <%= form.text_field :numero_boleta %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
